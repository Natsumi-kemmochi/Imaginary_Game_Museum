<div class="container bg-white">
  <div class="row justify-content-center">
   
    <p>新着投稿一覧</p>
    <%#共通テンプレート %>
    <%= render 'list', games: @games %>
    <%# ここまで %>

<%= paginate @games %>


  </div>
</div>
