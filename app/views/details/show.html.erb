<div class="container">
  <div class="row mt-3 p-2 bg-light justify-content-center">
    <%= image_tag @detail.image, size: "200x200" %>
    <br><br>
    <%= @detail.sub_text %>
    <br><br>

    <%= link_to "戻る", game_path(@game) %>
    <br><br>

    <%if @game.user_id == current_user.id %>
      <div>
        <%= link_to edit_game_detail_path(@detail.game_id, @detail.id), class: 'btn btn-light btn-outline-dark' do %>
         詳細の編集
        <% end %>
      </div>
      <br><br>
     
    <% end %>
  </div>
</div>